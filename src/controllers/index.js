// Import Controllers Hero after finish it's implementation and add to export object below.
// This way you only need to import index.js (controllers/index.js) to use all of them
import DemoController from './DemoController/DemoController';

export default {
    DemoController
}